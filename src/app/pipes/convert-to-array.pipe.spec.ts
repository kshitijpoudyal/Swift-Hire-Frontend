import { ConvertToArrayPipe } from './convert-to-array.pipe';

describe('ConvertToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
